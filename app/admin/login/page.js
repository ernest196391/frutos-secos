"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {supabase} from "../../../lib/supabase";import {tenant} from "../../../lib/tenant";
export default function Login(){const router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
async function submit(e){e.preventDefault();setBusy(true);setError("");const {error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error){setBusy(false);setError("No pudimos iniciar sesión. Revisa los datos.");return}const {data:ok}=await supabase.rpc("colo_is_admin");if(!ok){await supabase.auth.signOut();setBusy(false);setError("Esta cuenta no tiene acceso al panel.");return}router.replace("/admin")}
return <main className="adminLogin"><section className="adminLoginCard"><div className="adminLoginBrand"><img src={tenant.brand.lockup||tenant.brand.logo} alt={tenant.name}/></div><div className="adminLoginIntro"><span>Centro de control</span><h1>Administración</h1><p>Gestiona pedidos y operación de {tenant.shortName}.</p></div><form onSubmit={submit}><label>Correo<input type="email" autoComplete="username" inputMode="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Contraseña<input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="adminPrimary" disabled={busy}>{busy?"Entrando…":"Entrar al panel"}</button>{error&&<p className="adminLoginError" role="alert">{error}</p>}<Link className="adminForgot" href="/admin/recuperar">¿Olvidaste la contraseña?</Link></form></section></main>}