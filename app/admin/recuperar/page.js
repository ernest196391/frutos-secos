"use client";import {useState} from "react";import Link from "next/link";import {supabase} from "../../../lib/supabase";import {tenant} from "../../../lib/tenant";
export default function Recover(){const[email,setEmail]=useState(""),[sent,setSent]=useState(false),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:window.location.origin+"/admin/nueva-clave"});setBusy(false);setSent(true)}return <main className="adminLogin"><section className="adminLoginCard"><img src={tenant.logo} alt={tenant.name}/><h1>Recuperar acceso</h1>{sent?<><p>Si esa cuenta existe, recibirás un enlace para crear una contraseña nueva.</p><Link href="/admin/login">Volver</Link></>:<form onSubmit={submit}><label>Correo<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="adminPrimary" disabled={busy}>{busy?"Enviando…":"Mandarme el enlace"}</button><Link href="/admin/login">Volver</Link></form>}</section></main>}